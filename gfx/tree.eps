%!
%%BoundingBox: 147 438 419 521

/vsep 18 def
/hsep 72 def

0 setlinewidth

/tree { % call: n m tree - n deep, m wide at each level
	2 dict begin
	/m exch def
	/n exch def
	n 0 gt {
		1 1 m 1 sub {
			gsave
			newpath 0 0 moveto
			m 2 div sub hsep mul dup vsep neg rlineto stroke
			vsep neg translate
			1 m div 1 scale
			n 1 sub m tree
			grestore
		} for
	} if
	end
} def


250 500 translate
3 4 tree
/Times-Italic 10 selectfont
hsep 0.3 mul 0 moveto (Root) show
/Times-Roman 10 selectfont
hsep 1.1 mul vsep neg moveto (Paragraph-level items) show
hsep 1.3 mul vsep neg 2 mul moveto (Galley renderings) show
hsep 1.4 mul vsep neg 3 mul moveto (COG pointers) show


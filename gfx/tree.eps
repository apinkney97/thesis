%!
%%BoundingBox: 0 0 272 72

<<
  /HWResolution [600 600]
  /PageSize [272.000 72.000]
>> setpagedevice

/vsep 18 def
/hsep 72 def

0 setlinewidth

/tree { % call: n m tree - n deep, m wide at each level
	2 dict begin
	/m exch def
	/n exch def
	n 0 gt {
		1 1 m 1 sub {
			gsave
			newpath 0 0 moveto
			m 2 div sub hsep mul dup vsep neg rlineto stroke
			vsep neg translate
			1 m div 1 scale
			n 1 sub m tree
			grestore
		} for
	} if
	end
} def


100 60 translate
3 4 tree
/Palatino-Italic 8 selectfont
hsep 0.3 mul 0 moveto (Root) show
/Palatino-Roman 8 selectfont
hsep 1.1 mul vsep neg moveto (Paragraph-level items) show
hsep 1.3 mul vsep neg 2 mul moveto (Galley renderings) show
/Palatino-Roman 6 selectfont % Fake small caps... suck it typographers
hsep 1.4 mul vsep neg 3 mul moveto (COG) show
/Palatino-Roman 8 selectfont
( pointers) show

